# Ralph Progress - Industrial Edge Middleware
Branch: ralph/industrial-edge-middleware
Project: Industrial Edge Middleware - Multi-Tenant Data Acquisition System

## Progress Log

### 2025-01-24 - Initial Setup (Iterations 1-2)

#### Completed User Stories:
- **US-001**: Created migrations/001_init_schema.sql with organizations, sites, areas tables and proper indexes
- **US-002**: Created gateways, tags, alarms tables in migrations/001_init_schema.sql with all constraints
- **US-003**: Created internal/models/*.go with all structs (Organization, Site, Area, Gateway, Tag, Alarm) and internal/db/db.go with Connect() function
- **US-004**: Created internal/handlers/organizations.go with Create() and List() handlers, integrated into services/core-api/main.go
- **US-005**: Created internal/handlers/sites.go with Create() and List() handlers, integrated into services/core-api/main.go
- **US-006**: Created internal/handlers/areas.go with Create() and List() handlers, integrated into services/core-api/main.go
- **US-007**: Created internal/handlers/gateways.go with Create(), List(), Update() handlers, integrated into services/core-api/main.go

#### Files Created:
```
industrial-edge-middleware/
├── go.mod                          # Go module with all dependencies
├── migrations/
│   └── 001_init_schema.sql         # Complete database schema
├── internal/
│   ├── db/
│   │   └── db.go                   # Database connection layer
│   ├── models/
│   │   ├── organization.go         # Organization, Site, Area structs
│   │   ├── gateway.go              # Gateway struct
│   │   └── tag.go                  # Tag, Alarm structs
│   └── handlers/
│       ├── organizations.go        # Organizations API handler
│       ├── sites.go                # Sites API handler
│       ├── areas.go                # Areas API handler
│       └── gateways.go             # Gateways API handler (with Update)
└── services/
    └── core-api/
        └── main.go                 # Gin server on port 8080
```

#### Dependencies Added:
- github.com/gin-gonic/gin v1.10.0
- github.com/lib/pq v1.10.9
- github.com/redis/go-redis/v9 v9.7.0
- github.com/eclipse/paho.mqtt.golang v1.5.0
- github.com/influxdata/influxdb-client-go/v2 v2.13.0
- github.com/robinson/gos7 v0.0.0-20231202003052-2c9b4e7e7f23
- github.com/goburrow/modbus v0.0.0-20231103181958-23f9ac3b4482

#### Next Priority:
- **US-008**: Create tags REST API endpoints

#### Notes:
- Database schema is complete with proper indexes and constraints
- Go models are properly mapped to database tables
- REST API foundation is in place with Gin framework
- All code compiles successfully with `go build ./...`

### 2026-01-24 - Docker Infrastructure (Iteration 3)

#### Completed User Stories:
- **US-009**: Docker Compose configuration already existed with PostgreSQL, Redis, Mosquitto, InfluxDB - marked as complete
- **US-010**: Created services/core-api/Dockerfile with multi-stage build (golang:1.21-alpine -> alpine:3.19), added core-api service to docker-compose.yml with proper environment variables, health checks, and service dependencies

#### Files Created/Modified:
- `services/core-api/Dockerfile` - Multi-stage build for core-api
- `docker-compose.yml` - Added core-api service with depends_on and healthcheck

#### Next Priority:
- **US-011**: Create MQTT client library for Go

### 2026-01-24 - MQTT Client Library (Iteration 4)

#### Completed User Stories:
- **US-011**: Created internal/mqtt/client.go with reusable MQTT client library

#### Features Implemented:
- `NewClient(cfg Config)` - Creates MQTT client with configurable host, port, clientID, credentials
- `Connect()` - Establishes connection to Mosquitto broker
- `Disconnect(quiesce)` - Gracefully disconnects from broker
- `Publish(topic, payload)` - Publishes messages to MQTT topics (QoS 1)
- `PublishWithQoS(topic, payload, qos, retained)` - Publishes with custom QoS and retain flag
- `Subscribe(topic, callback)` - Subscribes to topics with message callback handler
- `SubscribeWithQoS(topic, callback, qos)` - Subscribes with custom QoS level
- `Unsubscribe(topic)` - Removes topic subscription
- Auto-reconnect with automatic resubscription to all previously subscribed topics
- Connection error handling with logging
- Last Will Testament (LWT) support for health monitoring
- Thread-safe subscription management with mutex

#### Files Created:
- `internal/mqtt/client.go` - MQTT client library using paho.mqtt.golang

#### Next Priority:
- **US-012**: Create driver-s7 service Docker container structure

### 2026-01-24 - S7 Driver Service Structure (Iteration 5)

#### Completed User Stories:
- **US-012**: Created driver-s7 service Docker container structure

#### Features Implemented:
- `services/driver-s7/Dockerfile` - Multi-stage build (golang:1.21-alpine -> alpine:3.19)
- `services/driver-s7/main.go` - S7 driver service with:
  - Gateway configuration loading from PostgreSQL by gateway ID
  - Full hierarchy info loading (org/site/area names) for MQTT topic construction
  - MQTT client with Last Will Testament (LWT) for health monitoring (`sys/health/{gateway_id}`)
  - Subscription to `sys/command/reload/{gateway_id}` for dynamic config reload
  - Non-blocking reload channel to prevent duplicate reload requests
  - Polling loop with configurable scan rate from gateway settings
  - `slugify()` function for URL-friendly topic names (spaces -> hyphens)
  - Graceful shutdown handling with SIGINT/SIGTERM signals
  - Environment variable configuration for DB and MQTT connections

#### Files Created:
- `services/driver-s7/Dockerfile` - Docker container definition
- `services/driver-s7/main.go` - S7 driver service main entry point

#### Next Priority:
- **US-013**: Implement S7 protocol connection and polling

### 2026-01-24 - S7 Protocol Implementation (Iteration 6)

#### Completed User Stories:
- **US-013**: Implemented S7 protocol connection and polling

#### Features Implemented:
- `internal/s7/client.go` - Pure Go S7 protocol implementation (no external dependencies)
- `NewClient(cfg Config)` - Creates S7 client with IP, rack, slot, port configuration
- `NewClientFromConfig(map[string]interface{})` - Creates client from connection_config JSONB
- `Connect()` - Establishes TCP connection with COTP/S7 handshake sequence
- `ConnectWithRetry()` - Connection with configurable retry count and interval
- `Disconnect()` - Clean connection teardown
- `IsConnected()` - Thread-safe connection status check
- `ReadTag(address, dataType)` - Reads values from PLC memory areas
- `ReadMultipleTags(addresses, dataTypes)` - Batch read helper

#### S7 Protocol Features:
- TPKT/COTP connection establishment
- S7 Communication Setup with PDU size negotiation
- S7 Read Variable request/response handling
- Proper sequencing with PDU reference numbers

#### Address Format Support:
- Data Blocks: `DB1.DBW0`, `DB1.DBD4`, `DB1.DBX0.0`
- Merker (Memory): `MW0`, `MD4`, `M0.0`
- Inputs: `IW0`, `ID4`, `I0.0`
- Outputs: `QW0`, `QD4`, `Q0.0`

#### Data Type Support:
- `BOOL` - 1-bit values with bit offset extraction
- `INT` - 16-bit signed integer (big-endian)
- `DINT` - 32-bit signed integer (big-endian)
- `REAL` - 32-bit IEEE 754 float (big-endian)

#### Error Handling:
- Connection errors trigger automatic disconnect detection
- Network errors (OpError, EOF) properly handled
- Read errors return quality=1 (bad) with error details
- Timeout configuration for connect and read operations

#### Files Created:
- `internal/s7/client.go` - S7 protocol client library

#### Next Priority:
- **US-014**: Implement S7 driver data publishing to MQTT

### 2026-01-24 - S7 Driver MQTT Publishing (Iteration 7)

#### Completed User Stories:
- **US-014**: Implemented S7 driver data publishing to MQTT

#### Features Implemented:
- S7 client integration in `services/driver-s7/main.go`
- `connectS7()` - Creates S7 client from gateway connection_config and connects with retry
- `poll()` - Reads all tags using S7 client and publishes to MQTT
- `publishTagValue()` - Publishes tag value to MQTT with JSON payload
- `hasValueChanged()` / `updatePreviousValue()` - Report by Exception implementation
- `valuesEqual()` - Type-safe value comparison for BOOL, INT, DINT, REAL

#### MQTT Topic Structure:
- Topic format: `data/{org}/{site}/{area}/{gateway}/{alias}`
- All names are slugified (lowercase, spaces to hyphens)

#### MQTT Payload Format:
```json
{
  "v": <value>,
  "ts": <timestamp_ms>,
  "q": <quality>
}
```
- `v`: The tag value (bool, int16, int32, float32)
- `ts`: Unix timestamp in milliseconds
- `q`: Quality (0 = good, 1 = bad)

#### Report by Exception (RBE):
- Stores previous values in thread-safe map `previousValues`
- Only publishes when value has changed from previous read
- First read for any tag is always published
- Type-specific comparison for accurate change detection

#### Error Handling:
- Auto-reconnect to S7 PLC on connection loss
- Bad quality (q=1) published when read errors occur
- Graceful S7 disconnect on service shutdown

#### Files Modified:
- `services/driver-s7/main.go` - Added S7 integration, MQTT publishing, RBE

#### Next Priority:
- **US-015**: Create driver-modbus service Docker container structure

### 2026-01-24 - Modbus Driver Service Structure (Iteration 8)

#### Completed User Stories:
- **US-015**: Created driver-modbus service Docker container structure

#### Features Implemented:
- `services/driver-modbus/Dockerfile` - Multi-stage build (golang:1.21-alpine -> alpine:3.19)
- `services/driver-modbus/main.go` - Modbus driver service with:
  - Gateway configuration loading from PostgreSQL by gateway ID
  - Full hierarchy info loading (org/site/area names) for MQTT topic construction
  - MQTT client with Last Will Testament (LWT) for health monitoring (`sys/health/{gateway_id}`)
  - Subscription to `sys/command/reload/{gateway_id}` for dynamic config reload
  - Non-blocking reload channel to prevent duplicate reload requests
  - Polling loop with configurable scan rate from gateway settings
  - `slugify()` function for URL-friendly topic names (spaces -> hyphens)
  - Graceful shutdown handling with SIGINT/SIGTERM signals
  - Environment variable configuration for DB and MQTT connections
  - Report by Exception support (hasValueChanged, updatePreviousValue, valuesEqual)
  - publishTagValue function ready for use when Modbus client is implemented

#### Files Created:
- `services/driver-modbus/Dockerfile` - Docker container definition
- `services/driver-modbus/main.go` - Modbus driver service main entry point

#### Next Priority:
- **US-016**: Implement Modbus TCP protocol connection and polling

### 2026-01-24 - Modbus TCP Protocol Implementation (Iteration 9)

#### Completed User Stories:
- **US-016**: Implemented Modbus TCP protocol connection and polling

#### Features Implemented:
- `internal/modbus/client.go` - Pure Go Modbus TCP implementation (no external dependencies)
- `NewClient(cfg Config)` - Creates Modbus client with IP, port, slave ID, timeout configuration
- `NewClientFromConfig(map[string]interface{})` - Creates client from connection_config JSONB
- `Connect()` - Establishes TCP connection with configurable timeout
- `ConnectWithRetry()` - Connection with configurable retry count and interval
- `Disconnect()` - Clean connection teardown
- `IsConnected()` - Thread-safe connection status check
- `ReadHoldingRegister(address)` - Reads holding register (function code 0x03)
- `ReadInputRegister(address)` - Reads input register (function code 0x04)
- `ReadTag(address, dataType)` - Unified tag reading interface

#### Modbus TCP Protocol Features:
- Proper MBAP header handling (Transaction ID, Protocol ID, Length, Unit ID)
- Transaction ID management with auto-increment
- Big-endian byte order for all register values
- Exception response detection and error handling

#### Address Format Support:
- Holding Registers: `HR100` for INT/REAL, `HR100.0` for BOOL (bit 0)
- Input Registers: `IR100` for INT/REAL, `IR100.0` for BOOL (bit 0)

#### Data Type Support:
- `INT` - 16-bit signed integer from single register
- `REAL` - 32-bit IEEE 754 float from 2 consecutive registers
- `BOOL` - Single bit from register (bit offset 0-15)

#### Error Handling:
- Connection errors set disconnected state
- Modbus exception responses properly detected
- Timeout configuration for connect and read operations
- Thread-safe connection state management with mutex

#### Files Created:
- `internal/modbus/client.go` - Modbus TCP protocol client library

#### Next Priority:
- **US-017**: Implement Modbus driver data publishing to MQTT

### 2026-01-24 - Modbus Driver MQTT Publishing (Iteration 10)

#### Completed User Stories:
- **US-017**: Implemented Modbus driver data publishing to MQTT

#### Features Implemented:
- Modbus client integration in `services/driver-modbus/main.go`
- `connectModbus()` - Creates Modbus client from gateway connection_config and connects with retry
- `poll()` - Reads all tags using Modbus client and publishes to MQTT
- `readTag()` - Reads a single tag value via modbus.ReadTag() and returns value with quality
- `publishTagValue()` - Publishes tag value to MQTT with JSON payload
- `hasValueChanged()` / `updatePreviousValue()` - Report by Exception implementation
- `valuesEqual()` - Type-safe value comparison for BOOL, INT, REAL

#### MQTT Topic Structure:
- Topic format: `data/{org}/{site}/{area}/{gateway}/{alias}`
- All names are slugified (lowercase, spaces to hyphens)

#### MQTT Payload Format:
```json
{
  "v": <value>,
  "ts": <timestamp_ms>,
  "q": <quality>
}
```
- `v`: The tag value (bool, int16, float32)
- `ts`: Unix timestamp in milliseconds
- `q`: Quality (0 = good, 1 = bad)

#### Report by Exception (RBE):
- Stores previous values in thread-safe map `previousValues`
- Only publishes when value has changed from previous read
- First read for any tag is always published
- Bad quality (q=1) is always published immediately
- Type-specific comparison for accurate change detection

#### Error Handling:
- Auto-reconnect to Modbus device on connection loss
- Bad quality (q=1) published when read errors occur
- Graceful Modbus disconnect on service shutdown

#### Files Modified:
- `services/driver-modbus/main.go` - Added Modbus integration, MQTT publishing, RBE

#### Next Priority:
- **US-018**: Create driver-manager service for container orchestration

### 2026-01-24 - Driver Manager Service (Iteration 11)

#### Completed User Stories:
- **US-018**: Created driver-manager service for container orchestration

#### Features Implemented:
- `services/driver-manager/Dockerfile` - Multi-stage build (golang:1.21-alpine -> alpine:3.19) with Docker CLI installed
- `services/driver-manager/main.go` - Driver manager service with:
  - Docker API client using github.com/docker/docker
  - PostgreSQL polling every 10 seconds for gateway enabled/disabled status
  - Container lifecycle management (start/stop/remove) based on gateway.enabled field
  - Docker network setup (industrial-network) for container-to-container communication
  - Thread-safe gateway state tracking with mutex-protected map
  - Container naming: `driver-s7-{id}` or `driver-modbus-{id}`
  - Image selection based on driver_type (S7 -> industrial-driver-s7:latest, MODBUS_TCP -> industrial-driver-modbus:latest)
  - Environment variable passthrough (DB_*, MQTT_*) to driver containers
  - Proper logging of all container operations (start/stop/create/remove)
  - Graceful shutdown with SIGINT/SIGTERM handling and container cleanup
  - Detection of deleted gateways (no longer in database) with automatic container stop

#### Docker Integration:
- Uses Docker socket bind mount (`/var/run/docker.sock:/var/run/docker.sock:ro`)
- Creates containers on `industrial-network` for service discovery
- Restart policy: `unless-stopped` for driver containers
- Health check dependency on postgres and mosquitto services

#### Gateway Orchestration Logic:
1. Poll PostgreSQL for all gateways (id, name, driver_type, enabled)
2. For each enabled gateway not currently running: start container
3. For each disabled gateway currently running: stop container
4. For gateways removed from database: stop container
5. Thread-safe state tracking prevents duplicate container creation

#### Files Created:
- `services/driver-manager/Dockerfile` - Docker container definition
- `services/driver-manager/main.go` - Driver manager service main entry point

#### Files Modified:
- `go.mod` - Added github.com/docker/docker v20.10.7+incompatible and github.com/docker/go-connections v0.4.0 dependencies
- `docker-compose.yml` - Added driver-manager service with Docker socket volume mount

#### Dependencies Added:
- github.com/docker/docker v20.10.7+incompatible
- github.com/docker/go-connections v0.4.0
- github.com/opencontainers/image-spec v1.1.1
- github.com/opencontainers/go-digest v1.0.0
- github.com/gogo/protobuf v1.3.2
- github.com/sirupsen/logrus v1.9.4
- google.golang.org/grpc v1.78.0
- github.com/gorilla/mux v1.8.1
- github.com/containerd/containerd v1.7.30
- And related transitive dependencies

#### Next Priority:
- **US-019**: Implement MQTT reload command for dynamic configuration

### 2026-01-24 - MQTT Reload Command for Dynamic Configuration (Iteration 12)

#### Completed User Stories:
- **US-019**: Implemented MQTT reload command for dynamic configuration

#### Features Implemented:
- MQTT client integration in `services/core-api/main.go`
  - Environment variables: MQTT_HOST (default: localhost), MQTT_PORT (default: 1883), MQTT_CLIENT_ID (default: core-api)
  - Auto-reconnect enabled with 30 second keepalive
  - Graceful disconnect on shutdown
  - Warning logged if MQTT connection fails (doesn't prevent API from starting)

- Updated `internal/handlers/gateways.go`:
  - Added `MQTTClient` interface with `Publish(topic, payload)` method
  - `GatewaysHandler` now accepts `MQTTClient` parameter
  - `Update()` handler publishes to `sys/command/reload/{gateway_id}` after successful gateway update
  - MQTT publish errors are silently handled (don't fail HTTP request)

- Updated `internal/handlers/tags.go`:
  - `TagsHandler` now accepts `MQTTClient` parameter
  - `Update()` handler publishes to `sys/command/reload/{gateway_id}` after successful tag update
  - Uses the gateway_id from the tag to determine which driver to reload

#### How It Works:
1. When a gateway is updated via `PUT /api/gateways/{id}`:
   - Gateway is updated in PostgreSQL
   - core-api publishes `"reload"` payload to `sys/command/reload/{gateway_id}`

2. When a tag is updated via `PUT /api/tags/{id}`:
   - Tag is updated in PostgreSQL
   - core-api publishes `"reload"` payload to `sys/command/reload/{tag.gateway_id}`

3. Driver services (already implemented in US-012 and US-015):
   - Subscribe to `sys/command/reload/{gateway_id}` topic
   - On receiving reload message, reload gateway config and tags from PostgreSQL
   - Update internal tag map without stopping polling loop
   - New tags are added, removed tags are deleted from polling

#### Design Decisions:
- MQTTClient interface allows dependency injection and testing
- MQTT publish failures don't fail HTTP requests (API resilience)
- Drivers already have non-blocking reload channel to prevent duplicate reloads
- Simple "reload" string payload - drivers just need to receive the message

#### Files Modified:
- `services/core-api/main.go` - Added MQTT client integration
- `internal/handlers/gateways.go` - Added MQTT client and reload publishing
- `internal/handlers/tags.go` - Added MQTT client and reload publishing

#### Next Priority:
- **US-020**: Create Redis cache package for Go

### 2026-01-24 - Redis Cache Package (Iteration 13)

#### Completed User Stories:
- **US-020**: Created Redis cache package for Go

#### Features Implemented:
- `internal/redis/client.go` - Redis client library wrapper around github.com/redis/go-redis/v9
- `NewClient(cfg Config)` - Creates Redis client with Host, Port, Password, DB configuration
- `Connect()` - Establishes connection with Ping verification
- `Disconnect()` - Gracefully closes Redis connection
- `Set(key, value, ttl)` - Stores key-value pair with expiration
- `Get(key)` - Retrieves value as string, returns error if key doesn't exist
- `GetInt(key)` - Type-safe helper for integer values
- `GetFloat(key)` - Type-safe helper for float values
- `Delete(key)` - Removes key from Redis
- `Exists(key)` - Checks if key exists (returns bool)
- `SetJSON(key, jsonStr, ttl)` - Helper for storing JSON strings
- `GetJSON(key)` - Helper for retrieving JSON strings
- `Keys(pattern)` - Returns all keys matching pattern
- `FlushDB()` - Clears all keys in current database (use with caution)
- `Ping()` - Health check connection
- `IsConnected()` - Returns true if connected

#### Auto-Reconnect Configuration:
- MaxRetries: 3
- MaxRetryBackoff: 2 seconds
- MinRetryBackoff: 100ms
- Connection pool size: 10
- Min idle connections: 5
- ConnMaxLifetime: 30 minutes
- ConnMaxIdleTime: 5 minutes

#### Files Created:
- `internal/redis/client.go` - Redis client wrapper with application-specific API

#### Files Modified:
- `go.mod` - Added github.com/redis/go-redis/v9 v9.17.2 and related dependencies

#### Next Priority:
- **US-021**: Create engine-historian service structure

### 2026-01-24 - Engine-Historian Service Structure (Iteration 14)

#### Completed User Stories:
- **US-021**: Created engine-historian service container structure

#### Features Implemented:
- `services/engine-historian/Dockerfile` - Multi-stage build (golang:1.21-alpine -> alpine:3.19)
- `services/engine-historian/main.go` - Engine historian service with:
  - MQTT client with subscription to `data/#` topic for all real-time data
  - InfluxDB client with WriteAPI (batch size: 1000, flush interval: 1000ms)
  - Redis client for caching (used in US-024 for real-time values)
  - Environment variable configuration:
    - `MQTT_HOST` (default: localhost), `MQTT_PORT` (default: 1883)
    - `REDIS_HOST` (default: localhost), `REDIS_PORT` (default: 6379)
    - `INFLUX_URL` (default: http://localhost:8086)
    - `INFLUX_TOKEN` (required)
    - `INFLUX_ORG` (default: industrial), `INFLUX_BUCKET` (default: historian)
  - `handleDataMessage(topic, payload)` callback for incoming MQTT messages
    - Parses topic structure: `data/{org}/{site}/{area}/{gateway}/{alias}`
    - Extracts hierarchy names for multi-tenant isolation
    - TODO in US-022: Parse JSON payload, apply deadband, buffer for batch write
  - `handleInfluxErrors()` goroutine for handling WriteAPI batch write errors
  - Graceful shutdown with SIGINT/SIGTERM handling
  - InfluxDB connection health check on startup

#### Files Created:
- `services/engine-historian/Dockerfile` - Docker container definition
- `services/engine-historian/main.go` - Historian service main entry point

#### Files Modified:
- `go.mod` - Added github.com/influxdata/influxdb-client-go/v2 v2.13.0 and related dependencies

#### Dependencies Added:
- github.com/influxdata/influxdb-client-go/v2 v2.13.0
- github.com/influxdata/line-protocol v0.0.0-20200327222509-2487e7298839
- github.com/google/uuid v1.3.1

#### Next Priority:
- **US-022**: Implement historian data buffering and batch write


### 2026-01-24 - Historian Data Buffering and Batch Write (Iteration 15)

#### Completed User Stories:
- **US-022**: Implemented historian data buffering and batch write

#### Features Implemented:
- `services/engine-historian/main.go` - Enhanced with buffering and batch write functionality
- `DataPoint` struct - Stores buffered data with:
  - Measurement: "tag_data"
  - Tags: organization, site, area, gateway, alias
  - Fields: value, quality
  - Timestamp in milliseconds
- `MQTTPayload` struct - Parses JSON from MQTT:
  - V (value): interface{} for bool, float64, int
  - Ts (timestamp): int64 in milliseconds
  - Q (quality): int (0=good, 1=bad)

#### Buffer Management:
- Thread-safe buffer using `sync.Mutex` to protect `[]*DataPoint` slice
- Pre-allocated buffer with capacity of 1000 points for efficiency
- Constants:
  - `bufferFlushSize = 1000` - Flush when buffer reaches this size
  - `bufferFlushInterval = 1 * time.Second` - Flush at regular intervals

#### Flush Mechanisms:
1. **Size-based flush**: `addToBuffer()` triggers flush when 1000 points accumulated
2. **Time-based flush**: `periodicFlush()` goroutine flushes every 1 second via ticker
3. **Shutdown flush**: Graceful shutdown flushes remaining buffer before exit

#### Functions Added:
- `addToBuffer(dp *DataPoint)` - Thread-safe buffer append with size-triggered flush
- `periodicFlush()` - Goroutine that flushes on ticker interval and shutdown
- `flushBuffer()` - Logs and triggers flush (assumes mutex held)
- `flushBufferUnsafe()` - Actual flush implementation without locking

#### MQTT Data Processing:
- `handleDataMessage()` now parses JSON payload from MQTT messages
- Extracts v, ts, q from JSON: `{"v": value, "ts": timestamp_ms, "q": quality}`
- Converts timestamp from milliseconds to nanoseconds for InfluxDB
- Creates InfluxDB points using `influxdb2.NewPoint()`
- Writes points to InfluxDB via `WriteAPI.WritePoint()`
- Forces write with `WriteAPI.Flush()`

#### Thread Safety:
- All buffer access protected by `bufferMutex`
- Separate unsafe function for flush operations when mutex is already held
- Safe concurrent access from MQTT callback and periodic flush goroutine

#### Files Modified:
- `services/engine-historian/main.go` - Added DataPoint, MQTTPayload structs; buffer management; periodic flush; JSON parsing

#### Next Priority:
- **US-023**: Implement historian deadband filtering


### 2026-01-24 - Historian Deadband Filtering (Iteration 16)

#### Completed User Stories:
- **US-023**: Implemented historian deadband filtering

#### Features Implemented:
- `services/engine-historian/main.go` - Enhanced with deadband filtering functionality
- PostgreSQL connection for querying tag configuration (historize, historize_deadband)
- New structs:
  - `TagInfo` - Holds tag ID, Historize flag, and HistorizeDeadband value
  - `PreviousValue` - Stores previous value in Redis for comparison

#### Deadband Filtering Logic:
1. **Tag Info Lookup** (`getTagInfo`):
   - Queries PostgreSQL via JOIN across organizations → sites → areas → gateways → tags
   - Matches by hierarchy names (org, site, area, gateway, alias) using case-insensitive comparison
   - Caches results in Redis for 1 hour (key: `tag_info:{org}:{site}:{area}:{gateway}:{alias}`)
   - Returns tag ID, historize flag, and historize_deadband value

2. **Historize Check**:
   - If `tag.historize` is false, data is skipped entirely (not stored in InfluxDB)

3. **Deadband Comparison** (`shouldStoreValue`):
   - If deadband ≤ 0, always store (filtering disabled)
   - Retrieves previous value from Redis (key: `prev_value:{tag_id}`)
   - First value always stored (no previous to compare)
   - Compares with new value using deadband threshold

4. **Value Comparison** (`exceedsDeadband`):
   - Supports float64, int, and bool value types
   - For numeric types: `|new_value - previous_value| >= deadband`
   - For boolean: any change stores the value
   - Uses `math.Abs()` for proper absolute difference calculation

5. **Previous Value Storage** (`storePreviousValue`):
   - After storing in InfluxDB buffer, updates Redis with new value
   - Uses same key for next comparison (`prev_value:{tag_id}`)
   - No expiration (overwritten on each new value)

#### Redis Keys Used:
- `tag_info:{org}:{site}:{area}:{gateway}:{alias}` - Cached tag config (TTL: 3600s)
- `prev_value:{tag_id}` - Previous value for deadband comparison (no TTL)

#### Environment Variables Added:
- `DB_HOST` (default: localhost)
- `DB_PORT` (default: 5432)
- `DB_USER` (default: postgres)
- `DB_PASSWORD` (default: postgres)
- `DB_NAME` (default: industrial_edge)

#### Files Modified:
- `services/engine-historian/main.go` - Added deadband filtering; PostgreSQL connection; tag info caching
- `docker-compose.yml` - Added engine-historian service with PostgreSQL environment variables and dependencies

#### Next Priority:
- **US-024**: Implement historian Redis cache for real-time values

